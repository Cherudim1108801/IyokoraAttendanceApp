<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="IyokoraAttendanceApp.Views.OnboardingPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:IyokoraAttendanceApp.ViewModels"
    xmlns:models="clr-namespace:IyokoraAttendanceApp.Models"
    x:DataType="vm:OnboardingViewModel"
    Shell.NavBarIsVisible="False"
    Title="プロフィール登録">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20" VerticalOptions="Center">

            <Label
                Text="ようこそ"
                FontSize="28"
                FontAttributes="Bold"
                HorizontalOptions="Center" />
            <Label
                Text="出席管理アプリを使うために、表示名とパートを登録してください。"
                FontSize="14"
                TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center" />

            <Label Text="名前" FontAttributes="Bold" Margin="0,16,0,0" />
            <Entry Text="{Binding Name}" Placeholder="例：山田 太郎" />

            <Label Text="パート" FontAttributes="Bold" Margin="0,8,0,0" />
            <Picker
                ItemsSource="{Binding PartOptions}"
                ItemDisplayBinding="{Binding Label}"
                SelectedItem="{Binding SelectedPart}" />

            <Label
                Text="{Binding ErrorMessage}"
                TextColor="{StaticResource Magenta}"
                IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

            <Button
                Text="登録して始める"
                Command="{Binding SaveCommand}"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                Margin="0,16,0,0" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
